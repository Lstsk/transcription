[project]
name = "whisper-transcription"
version = "0.1.0"
description = "Offline video transcription with speaker diarization"
readme = "README.md"
requires-python = ">=3.9, <3.13"
dependencies = [
    "python-dotenv>=1.2.1",
    "whisperx>=3.7.5",
]
